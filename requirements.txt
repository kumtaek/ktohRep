# 소스 분석기 1단계 필수 패키지

# 데이터베이스
sqlalchemy>=1.4.0


# 설정 및 로깅
pyyaml>=6.0
python-dotenv>=0.19.0

# 비동기 처리
asyncio
aiofiles>=0.8.0

# Java 파싱 (선택적 - 없으면 정규식으로 폴백)
javalang>=0.13.0

# XML 처리 (Python 내장 xml.etree.ElementTree 사용)

# 정규식 및 텍스트 처리 (Python 내장)

# 수학 및 해싱 (Python 내장)

# 개발 및 테스트
pytest>=7.0.0
pytest-asyncio>=0.20.0

# 옵셔널 패키지들 (향후 확장용)

# Oracle 데이터베이스 연결 (운영 환경용)
# cx_oracle>=8.3.0

# JSQLParser Python 바인딩 (있는 경우)
# jsqlparser>=0.1.0

# Tree-sitter (향후 다국어 파서용)
# tree_sitter>=0.20.0

# ANTLR Python 런타임 (PL/SQL 파서용) 
# antlr4-python3-runtime>=4.9.0

# LLM 클라이언트 (2단계용)
# openai>=1.0.0
# langchain>=0.1.0

# 벡터 스토어 (2단계용)
# faiss-cpu>=1.7.0
# sentence-transformers>=2.2.0

# 웹 프레임워크 (2단계 API용)
# fastapi>=0.100.0
# uvicorn>=0.20.0